#include<stdio.h>
#include<stdlib.h>

void fun(char *str,...)
{
  float a;
  int b;
  char string[50];
  if(1)
  {

  if( sscanf(argv[1],"%f", &a) == 1 )
    printf("first arg is a float\n"); 
       format(str,...);

  else if(sscanf(argv[1],"%d", &b) == 1)
    printf("first argument is of type int\n");





  else if(sscanf(argv[1],"%s", string) == 1)
    printf("first argument is of type string\n");
 
     }


   if(1)

    {

  if( sscanf(argv[2],"%f", &a) == 1 )
     printf("second arg is a float\n"); 

  else if(sscanf(argv[2],"%d", &b) == 1)
     printf("second argument is of type int\n");

  else if(sscanf(argv[2],"%s", string) == 1)
     printf("second argument is of type string\n");
      
        }

}


format(char *str, ...)
{

 va_list list;
 

/*
while (*formatPtr)
	{
		if (*formatPtr != RAT('{'))
		{
			dst->Append(formatPtr, 1);
			formatPtr++;
			formatLen--;
			continue;
		}
		// skip { char
		formatPtr++;
		formatLen--;
*/
}

main()
{

   



    }
